#include <iostream>

int main()
{
    int a, b,;

    for (int i = 0; i < 5; i++)
    {
        std::cin >> a;

        if (a < 40)
        {
            a = 40;
        }
        int N[a];
        int sum = 0;
        
        std::cout << sum / 5 << std::endl;
    }
}